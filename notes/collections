collections data types
-----------------------
- data types which allow us to store multiple values into a single variable

- 4 collection data types - list, tuple, set & dictionary


List - []
-----
- mutable
- can be updated and modified.
- ordered, indexed
- duplicates are accepted
- ordered - elements are stored in the same given order

tuple - ()
-----
- immutable
- cannot update/modify
- ordered, indexed
- duplicates are accepted
- ordered - elements are stored in the same given order

Set - {}
--------
- mutable - we can update and modify the elements in a set
- unordered, un-indexed, un-duplicated
- duplicates are ignored
- unordered - elements are stored in random order


dictionary
----------
- stores the multiple values in key value pairs

list, tuple, set - group of values
dictionary - group of key value pairs

dictionary is mutable
we can update and modify the elements


list, tuple, set & dict (comparison)
-------------------------------------
[v1,v2,v3], (v1,v2,v3), {v1,v2,v3}, {k1:v1,k2:v2,k3:v3}

Indexing, Indexing, No Indexing, No Indexing

Ordered (same order), Ordered (same order), Un-ordered (Random order), Ordered (same order)

Mutable, Immutable, Mutable, Mutable

Duplicates are accepted, Duplicates are accepted,


















